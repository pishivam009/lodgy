{
  "id": "LODGY-63",
  "epic": "Property Setup",
  "title": "Deleting a floor or room that still has a tenant crashes the app",
  "description": "Found while auditing delete coverage. Deleting a floor whose bed still has an ACTIVE tenancy hard-crashes the app to the launcher - it does not fail gracefully, show an error, or block the action. REPRO: hostel Moonlight > floor Ground > Room 501 > Bed A occupied by a tenant with one invoice. Property > Moonlight > delete Ground > Delete. The confirmation reads 'Ground and any rooms/beds under it will be removed' with no mention that someone lives there; on confirming, the process dies. LOG: android.database.sqlite.SQLiteConstraintException: FOREIGN KEY constraint failed (code 787 SQLITE_CONSTRAINT_FOREIGNKEY), thrown from FloorDao_Impl.delete. ROOT CAUSE: the cascade chain is only half wired. Hostel -CASCADE-> Floor -CASCADE-> Room -CASCADE-> Bed, but Bed <- TenancyAgreement is NO ACTION, as are TenancyAgreement <- Invoice and Invoice <- Payment. So deleting a floor tries to cascade beds out from under agreements that still reference them, and SQLite refuses. RoomListViewModel already hard-blocks deleting a room with an OCCUPIED bed, which is why the room path does not crash - FloorListViewModel has no equivalent check. The developer flagged exactly this on LODGY-57 ('deleting a floor still cascades to rooms/beds with NO occupied-bed check, unlike room delete which hard-blocks - worth its own ticket'); this ticket is that ticket, now with a confirmed crash behind it. NOTE: this is not only a floor problem. Any future delete that removes beds out from under an agreement will hit the same constraint, which is why LODGY-64 should not add a hostel delete button until this is settled.",
  "acceptanceCriteria": [
    "Deleting a floor that contains an occupied bed no longer crashes - it is blocked with a clear explanation naming the tenant(s) in the way, matching how room delete already hard-blocks",
    "The block names what is in the way rather than failing generically, so the warden knows to check the tenant out first",
    "A floor with no occupied beds still deletes normally, with its existing confirmation",
    "The same guard is applied anywhere else a delete can remove a bed that an agreement still references, so the fix is not floor-only",
    "A test covers the occupied-bed case at the ViewModel or DAO level, so the crash cannot regress silently"
  ],
  "status": "Testing",
  "priority": "High",
  "assigneeRole": "Developer",
  "createdAt": "2026-09-04T12:05:59+00:00",
  "updatedAt": "2026-09-04T20:05:23+00:00",
  "history": [
    {
      "timestamp": "2026-09-04T12:05:59+00:00",
      "actor": "Tester",
      "action": "created",
      "note": "Filed by the Tester after reproducing a hard crash on the emulator: deleting a floor with an occupied bed dies with SQLITE_CONSTRAINT_FOREIGNKEY. Stack trace and repro in the description. This is the ticket the developer asked for when handing off LODGY-57."
    },
    {
      "timestamp": "2026-09-04T19:51:37+00:00",
      "actor": "Developer",
      "action": "status_change",
      "from": "Todo",
      "to": "In Progress",
      "note": "Starting. Copying the RoomListViewModel hard-block pattern one level up to FloorListViewModel, plus a DAO query for whether a floor holds an occupied bed. Presentation and guard only - no schema change expected."
    },
    {
      "timestamp": "2026-09-04T20:05:23+00:00",
      "actor": "Developer",
      "action": "status_change",
      "from": "In Progress",
      "to": "Testing",
      "note": "Fixed. FloorListViewModel gained the request/confirm/block cycle that RoomListViewModel already had: requestDelete() asks the DAO who lives under the floor and either raises a block or a plain confirmation, so floorRepository.delete() is now unreachable while an ACTIVE tenancy points at a bed below it. Two new DAO queries (BedDao.getActiveTenantNamesOnFloor / getActiveTenantNamesInRoom) return the tenant NAMES rather than a boolean, because AC2 asks the block to say who is in the way. FloorListScreen's delete dialog moved off local remember{} state onto the ViewModel, and gained a second dialog for the blocked case. AC4 applied to the room path too rather than leaving it floor-only: RoomListViewModel's existing hard-block now carries names as well, so both levels read the same way instead of one saying 'a bed is occupied' and the other naming people. New strings in values/ and values-hi/. VERIFIED ON EMULATOR by reproducing the exact crash path: Moonlight > Ground (0 vacant / 1 occupied, Priya) > delete. Previously this died to the launcher with SQLiteConstraintException FOREIGN KEY from FloorDao_Impl.delete; now the app stays alive (PID unchanged, zero FATAL in logcat) and shows 'Ground still has tenants living on it: Priya'. AC3 checked separately - created an empty floor, deleted it, and it took the ordinary confirmation and removed cleanly with no crash. BACKWARD COMPATIBILITY: not schema-affecting. No entity changed, database stays at version 4, no migration needed - the new queries are reads over existing tables. TESTS: 6 new in FloorListViewModelTest, including one asserting the repository is never reached for a tenanted floor, which is the regression guard for the crash itself. Three RoomListViewModelTest cases updated for the names-carrying block. Full suite 428 pass / 0 fail. Coverage 96.96% line."
    }
  ]
}
