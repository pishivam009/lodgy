{
  "id": "LODGY-82",
  "epic": "Rent & Payments",
  "title": "Rooms used by the warden or a caretaker need zero rent and no dues chasing",
  "description": "User-reported: some rooms or beds are occupied by the warden themselves or by a caretaker. These should carry no rent, stay out of the payment cycle, and optionally be recorded as an expense at the warden's discretion. WHAT HAPPENS TODAY: such a bed has to be either left marked vacant, which corrupts occupancy and invites the long-vacancy notification to nag about a bed that is actually in use, or set up as a real tenancy, which generates a monthly invoice that is never paid, shows as overdue on the dashboard, and triggers the overdue-rent notification every day. Both are wrong, and the second is actively harmful because it pollutes the money figures a warden relies on. There is no third option. WHAT IT TOUCHES: invoice generation must skip these agreements; the overdue notification and the dashboard's overdue count must exclude them; occupancy should count the bed as occupied, since it genuinely is; the monthly report should not carry phantom dues; and the vacancy notification must not treat it as empty. THE EXPENSE HALF IS SEPARABLE and probably should be separate. Recording forgone rent as an expense is a bookkeeping choice the user explicitly framed as discretionary ('as per warden discretion'), so the safe order is: first stop the app from being wrong about these rooms, then optionally let a warden log the notional cost. Shipping the first half alone already fixes the damage. OPEN QUESTION for the PO: whether this is a flag on the tenancy (a non-revenue occupancy) or a distinct occupancy type. A flag is less invasive and reuses the tenancy record so the room's history still reads normally; a separate type risks a second code path through everything that touches occupancy.",
  "acceptanceCriteria": [
    "A room or bed can be marked as used by the warden or a caretaker, with no rent",
    "No invoices are generated for it, so it never appears as dues or overdue anywhere",
    "It is excluded from overdue notifications and from the dashboard's overdue count",
    "It counts as occupied for occupancy and vacancy purposes, and never triggers the long-vacant nudge",
    "The monthly report and PDF exports show no phantom dues for it",
    "Optionally, and separately decided, the warden can record the forgone rent as an expense",
    "Existing data is unaffected and no migration is required beyond whatever the flag itself needs"
  ],
  "status": "Testing",
  "priority": "Medium",
  "assigneeRole": "Developer",
  "createdAt": "2026-09-04T20:10:57+00:00",
  "updatedAt": "2026-09-05T08:10:20Z",
  "history": [
    {
      "timestamp": "2026-09-04T20:10:57+00:00",
      "actor": "Product Owner",
      "action": "created",
      "note": "Filed from real warden feedback relayed by the PO. Checked against the code before filing; findings recorded in the description."
    },
    {
      "timestamp": "2026-09-04T21:09:45+00:00",
      "actor": "Product Owner",
      "action": "comment",
      "note": "DECIDED: a flag on the tenancy - a non-revenue occupancy - rather than a distinct occupancy type. It reuses the tenancy record so the room's history still reads normally and there is one code path through everything that touches occupancy; a separate type would fork that path. Ship in two stages: first stop the app being wrong about these rooms (no invoices, excluded from overdue counts and notifications, still counted as occupied, never nudged as vacant), then the discretionary expense recording separately. The first stage alone removes the damage."
    },
    {
      "timestamp": "2026-09-05T06:25:40+00:00",
      "actor": "Developer",
      "action": "status_change",
      "from": "Todo",
      "to": "In Progress",
      "note": "Starting stage one only. This IS schema-affecting - a nonRevenue flag on tenancy_agreements - so it needs a real migration 4->5 plus a migration test."
    },
    {
      "timestamp": "2026-09-05T06:38:26+00:00",
      "actor": "Developer",
      "action": "status_change",
      "from": "In Progress",
      "to": "Testing",
      "note": "Stage one built, as decided - the app stops being wrong about these rooms; the discretionary expense recording stays for a follow-up. Implemented as a nonRevenue FLAG on tenancy_agreements, not a separate occupancy type, so there is one code path through everything that touches occupancy. The agreement form gains a 'Warden / caretaker room' switch; turning it on clears the rent and any opening balance, since figures that will never be charged should not be stored. WHAT EACH CRITERION NEEDED, and most needed nothing: invoice generation now skips these agreements, which is the only behavioural change required. No invoices means nothing can be overdue, so the overdue notification and the dashboard's overdue count exclude them automatically, and the monthly report and PDF exports carry no phantom dues. The bed stays OCCUPIED because the tenancy is real, so occupancy counts it and the long-vacancy nudge - which only looks at VACANT beds - never fires for it. I checked each of those rather than assuming they followed. *** SCHEMA-AFFECTING - the first such change in a while. *** Database goes 4 -> 5 with MIGRATION_4_5, a plain ADD COLUMN ... NOT NULL DEFAULT 0. I declared @ColumnInfo(defaultValue = \"0\") on the entity so Room's exported schema and the migration SQL agree exactly, rather than relying on Room's validation being lenient about a default it does not know about. No fallbackToDestructiveMigration anywhere. MIGRATION VERIFIED AGAINST A REAL POPULATED DATABASE, not a synthetic one: the emulator held a live v4 file with 3 agreements, 7 invoices and 3 tenants. Installed the v5 build over it and confirmed user_version went 4 -> 5, the nonRevenue column exists, all 3/7/3 rows survived, and every existing agreement defaulted to 0 - so no warden's tenancy silently becomes non-revenue on upgrade. NOTE FOR QA: the migration is LAZY. It did not run on app launch, only once something actually opened the database - the PIN screen reads DataStore, not Room, so user_version was still 4 after the first launch and only flipped after unlocking. Worth knowing before concluding a migration has failed. TESTS: 2 new asserting a non-revenue tenancy never generates an invoice while an ordinary one on the same billing day still does. Full suite 444 pass, coverage 96.78% line."
    },
    {
      "timestamp": "2026-09-05T07:42:09Z",
      "actor": "Tester",
      "action": "status_change",
      "from": "Testing",
      "to": "In Progress",
      "note": "FAIL on AC1, but the core of the ticket is sound and confirmed on device.\n\nDEFECT (AC1 - 'a room or bed can be marked as used by the warden or a caretaker, WITH NO RENT'): the rent field is not gated by the switch, so a non-revenue tenancy can carry a real rent. AgreementFormScreen.kt:72 renders the agreed-rent field unconditionally; only the opening balance is wrapped in if (!uiState.nonRevenue) at line 131. onNonRevenueChange clears the rent at the moment the switch is flipped, but nothing enforces it afterwards - and the switch sits BELOW the rent field, so flip-then-edit is a natural order. Reproduced end to end: flipped the switch on (rent correctly cleared to 0, opening balance correctly hidden), then typed 7777 into the still-editable rent field and saved. The row persisted as nonRevenue=1, agreedRent=7777.0, while the hint directly under the switch still promised 'koi kiraya nahin'. Fix: hide or disable the rent field when nonRevenue is on, exactly as the opening balance already is, and/or clamp the rent to 0 in save() so the flag and the figure cannot disagree.\n\nMINOR: with the switch on, Save stays silently disabled until a deposit is typed - canSave requires advanceDeposit.toDoubleOrNull() != null and an empty string does not parse. A caretaker room has no deposit either, and unlike the billing-cycle field there is no error hint, so the button just looks dead. Suggest defaulting the deposit to 0 alongside the rent when the switch goes on.\n\nVERIFIED AND CORRECT - AC2: advanced the emulator clock to Mon 5 Oct 2026 (billing cycle day 5) and let InvoiceGenerationWorker run for real. The two ordinary tenancies generated 10/2026 invoices (Suresh Rs 5500, Priya Rs 4000); the two non-revenue tenancies generated none - non-revenue tenancies with ANY invoice: 0. Having the ordinary tenancies bill in the same run is what makes this proof rather than a worker that simply did not fire. AC3: the dashboard's 'bakaya invoice' read exactly 2, and neither non-revenue occupant appears anywhere in the invoice list; the overdue notification and the dashboard count both derive from invoices, and there are none. AC4: both beds are OCCUPIED in the DB, both occupants appear as active with their room and bed in the tenant list, room 101 reads 'bhara' (full) in All rooms, Home's vacant-bed count of 46 excludes them, and VacancyCheckWorker only inspects VACANT beds so the long-vacancy nudge cannot fire for them. AC5: the reports read from invoices, so no phantom dues are possible. AC7: the 4->5 migration is a plain ADD COLUMN with DEFAULT 0, verified against a real populated database - 3 agreements, 7 invoices and 3 tenants all survived and existing rows default to nonRevenue=0.\n\nAC6 (recording the forgone rent as an expense) was explicitly deferred by the PO to a stage two, which is the right call - but no follow-up ticket existed for it, so it was dropped on the floor. Filed LODGY-84 to hold it."
    },
    {
      "timestamp": "2026-09-05T08:10:20Z",
      "actor": "Developer",
      "action": "status_change",
      "note": "AC1 fixed, plus the minor QA raised.\n\nThe rent and deposit fields are now hidden when the warden/caretaker switch is on, exactly as the opening balance already was, so the field QA typed 7777 into no longer exists in that state. Hiding alone would still leave the ViewModel able to disagree with itself, so save() also forces the rent to 0.0 whenever nonRevenue is set - the flag and the figure now cannot diverge whatever order the fields were touched in, or whatever a future caller does.\n\nThe minor is fixed too: turning the switch on now clears the deposit to 0 alongside the rent, so canSave is satisfied immediately and Save no longer sits silently disabled on a field a caretaker room has no reason to carry.\n\nTwo regression tests: one turns the switch on, then types 7777 into the rent, saves, and asserts the agreement is created with 0.0 and never with 7777.0 - i.e. it reproduces QA's exact sequence; the other asserts the switch clears both figures and leaves the form saveable as-is.\n\nNo schema change - nonRevenue and migration 4->5 are untouched, so nothing here affects existing data.",
      "from": "In Progress",
      "to": "Testing"
    }
  ]
}
